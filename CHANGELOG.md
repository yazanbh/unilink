# UniLink - سجل التحديثات

## الإصدار 2.0.0 - التحديث الرئيسي

### التغييرات الجذرية

#### 1. إزالة Drizzle و MySQL
- تم إزالة جميع ملفات وتبعيات Drizzle ORM
- تم إزالة مجلد `drizzle/` بالكامل
- تم تحديث `package.json` لإزالة سكربت `db:push`
- **المشروع الآن يعتمد كلياً على Firebase Firestore**

#### 2. محرر الثيمات المرئي (Visual Theme Editor) ✨ جديد
تم إضافة محرر ثيمات متكامل يتيح للمستخدمين تخصيص مظهر صفحاتهم الشخصية:

**الملفات الجديدة:**
- `client/src/components/ThemeEditor.tsx` - المكون الرئيسي للمحرر
- `client/src/pages/ThemeCustomizer.tsx` - صفحة محرر الثيمات

**الميزات:**
- تخصيص الخلفية (لون صلب أو تدرج لوني)
- اختيار اتجاه التدرج (8 اتجاهات مختلفة)
- ألوان جاهزة (Presets) للتطبيق السريع
- تخصيص البطاقات:
  - لون الخلفية والشفافية
  - تأثير الزجاج (Glassmorphism)
  - استدارة الحواف وسمك الإطار
  - الظلال (6 مستويات)
- تخصيص النصوص:
  - 8 خطوط مختلفة (تشمل خطوط عربية)
  - لون النص الرئيسي والثانوي
- تأثيرات متحركة (Pulse, Gradient, Glow)
- معاينة فورية (Desktop/Mobile)
- حفظ الثيم في Firestore

**دوال Firebase الجديدة:**
- `saveCustomTheme()` - حفظ ثيم مخصص جديد
- `updateCustomTheme()` - تحديث ثيم موجود
- `getUserCustomTheme()` - جلب ثيم المستخدم
- `getCustomThemeById()` - جلب ثيم بالمعرف
- `deleteCustomTheme()` - حذف ثيم

#### 3. نظام فحص الروابط (Link Scanning) 🔒 جديد
تم إضافة نظام أمان متكامل لفحص الروابط قبل إضافتها:

**الملفات الجديدة:**
- `client/src/components/LinkScanner.tsx` - مكونات فحص الروابط

**المكونات:**
- `LinkScannerInline` - عرض حالة الأمان بشكل مضمن
- `LinkScannerDialog` - حوار فحص الرابط قبل الإضافة
- `BulkLinkScanner` - فحص جميع الروابط دفعة واحدة

**الفحوصات الأمنية:**
- التحقق من صحة تنسيق URL
- كشف الأنماط المشبوهة (phishing, malware, scam)
- كشف روابط IP المباشرة
- كشف النطاقات الفرعية المفرطة
- كشف النطاقات المشابهة (Lookalike domains)
- كشف مخططات URL الخبيثة (data:, javascript:)

**دوال Firebase الجديدة:**
- `checkLinkSafety()` - فحص أمان رابط واحد
- `checkMultipleLinksSafety()` - فحص روابط متعددة
- `storeLinkScanResult()` - تخزين نتيجة الفحص
- `reportUnsafeLink()` - الإبلاغ عن رابط غير آمن
- `getUnsafeLinkReports()` - جلب تقارير الروابط غير الآمنة
- `updateUnsafeLinkReportStatus()` - تحديث حالة التقرير

#### 4. تحديثات واجهة المستخدم
- إضافة رابط "محرر الثيم" في القائمة الجانبية
- إضافة زر "فحص الروابط" في صفحة إدارة الروابط
- تغيير زر "إضافة رابط" إلى "فحص وإضافة" مع أيقونة الدرع
- إضافة مسار `/dashboard/theme-editor` في التطبيق

### الملفات المعدلة
- `package.json` - إزالة سكربت db:push
- `client/src/App.tsx` - إضافة مسار محرر الثيمات
- `client/src/components/DashboardLayout.tsx` - إضافة رابط محرر الثيمات
- `client/src/pages/LinksManager.tsx` - دمج نظام فحص الروابط
- `client/src/lib/firebase.ts` - إضافة دوال الثيمات وفحص الروابط
- `client/src/pages/admin/AdminContent.tsx` - إضافة استيراد دوال تقارير الروابط

### الملفات المحذوفة
- `drizzle/` - مجلد Drizzle بالكامل
- `drizzle.config.ts` - ملف إعدادات Drizzle

---

## كيفية الاستخدام

### محرر الثيمات
1. سجل الدخول إلى حسابك
2. اذهب إلى لوحة التحكم
3. اضغط على "محرر الثيم" من القائمة الجانبية
4. خصص الألوان والخطوط والتأثيرات
5. شاهد المعاينة الفورية
6. اضغط "حفظ" لتطبيق التغييرات

### فحص الروابط
1. عند إضافة رابط جديد، اضغط "فحص وإضافة"
2. سيتم فحص الرابط تلقائياً
3. إذا كان آمناً، يمكنك إضافته مباشرة
4. إذا كان مشبوهاً، ستظهر التحذيرات مع خيار الإضافة على أي حال
5. يمكنك فحص جميع روابطك بالضغط على "فحص الروابط"

---

## ملاحظات تقنية

### Firebase Collections الجديدة
- `customThemes` - تخزين الثيمات المخصصة
- `linkScans` - تخزين نتائج فحص الروابط
- `unsafeLinkReports` - تقارير الروابط غير الآمنة

### الأنواع الجديدة (TypeScript)
```typescript
interface CustomTheme {
  id: string;
  uid: string;
  name: string;
  backgroundColor: string;
  backgroundGradient: boolean;
  gradientFrom: string;
  gradientTo: string;
  gradientDirection: string;
  cardBackgroundColor: string;
  cardOpacity: number;
  cardBlur: number;
  cardBorderRadius: number;
  cardShadow: string;
  textColor: string;
  textSecondaryColor: string;
  fontFamily: string;
  enableAnimation: boolean;
  animationType: string;
  enableGlassmorphism: boolean;
}

interface LinkScanResult {
  url: string;
  isSafe: boolean;
  threats: string[];
  checkedAt: Date;
  source: string;
}
```

---

تم التحديث بتاريخ: يناير 2026
